node {
  stage('========== Clone repository ==========') {
    checkout scm
  }
  stage('========== Build image ==========') {
    echo "Build image"
  }
  stage('========== Push image ==========') {
    echo "Build image"
  }
}
