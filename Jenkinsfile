node {
  stage('========== Clone repository ==========') {
    checkout scm
  }
  stage('========== Build image ==========') {
    # app = docker.build("jenkins-docker-pipeline/my-image")
    echo "Build image"
  }
  stage('========== Push image ==========') {
    echo "Build image"
  }
}
