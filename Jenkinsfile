node {
  stage('========== Clone repository ==========') {
    checkout scm
  }
  stage('========== Build image ==========') {
    echo "Build image"
  }
  stage('========== docker login ==========') {
    echo "docker login"
  }  
  stage('========== Push image ==========') {
    echo "Build image"
  }
}
